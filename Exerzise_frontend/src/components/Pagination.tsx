const Pagination = () => {
  // const perpage = 10;
  // const [count, setCount] = useState(100); //coach count
  // const [paginationNums, setPaginationNums] = useState(count / perpage);
  // return <div className="my-10">{paginationNums}</div>;
};

export default Pagination;
